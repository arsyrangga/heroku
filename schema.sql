-- SQLite
-- SQLite
-- SQLite
-- CREATE TABLE users(
--     id integer PRIMARY KEY,
--     username varchar NOT NULL UNIQUE,
--     password varchar NOT NULL,
--     avatar varchar,
--     created_at CURRENT_TIMESTAMP
-- );

--     id integer PRIMARY KEY,
--     title varchar NOT NULL,
--     body varchar NOT NULL,
--     user varchar NOT NULL,
--     created_at CURRENT_TIMESTAMP
-- );

-- INSERT INTO users(username, password, avatar) VALUES("rangga122", "rangga105", "/assets/images/background.jpg");

INSERT INTO notes(title, body, user) VALUES("Ini title 3", "Ini Isi Body 3", "Prawira");